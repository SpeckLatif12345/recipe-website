let leftButton = document.getElementById("nav-rightBtn");
let links = document.getElementById("links-container");

leftButton.addEventListener("click", function () {
  if (links.classList.contains("showLinks")) {
    links.classList.remove("showLinks");
    return;
  } else links.classList.add("showLinks");
  {
    return;
  }
});
